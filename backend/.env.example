# ------------------------------------------------------------------
#          ENVIRONMENT VARIABLES FOR Poly-Pro Backend Service
# ------------------------------------------------------------------
#
# This file serves as a template for the required environment variables.
# For local development, copy this file to `.env.local` and fill in the values.
# DO NOT commit the `.env.local` file to version control.
#
# Required for Step 2: Database Setup
# The full connection string for your PostgreSQL database (e.g., from Supabase).
DATABASE_URL="postgresql://user:password@host:port/dbname"

# Other variables will be added in subsequent steps.
