# ------------------------------------------------------------------
#          ENVIRONMENT VARIABLES FOR Poly-Pro Frontend
# ------------------------------------------------------------------
#
# This file serves as a template for the required environment variables.
# For local development, copy this file to `.env.local` and fill in the values.
# DO NOT commit the `.env.local` file to version control.

# ------------------------------------------------------------------
# Step 7: Clerk Authentication
# ------------------------------------------------------------------
# These keys are obtained from your Clerk application dashboard.
# The publishable key is safe to be exposed on the client-side.
# The secret key must be kept private and is used by Next.js middleware for server-side authentication.

NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_..."
CLERK_SECRET_KEY="sk_test_..."

# URL for the sign-in page, used by Clerk for redirects.
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in

# URL for the sign-up page, used by Clerk for redirects.
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# URL to redirect users to after they sign in.
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/markets

# URL to redirect users to after they sign up.
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/markets

# ------------------------------------------------------------------
# Step 9: Centralized API Service
# ------------------------------------------------------------------
# The base URL for the backend API. This must be exposed to the client,
# hence the NEXT_PUBLIC_ prefix.
# Using Railway hosted backend for both testing and production.
NEXT_PUBLIC_API_URL=
